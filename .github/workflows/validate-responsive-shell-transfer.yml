name: Validate responsive Shell transfer

on:
  push:
    branches:
      - fix/responsive-shell-transfer

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Ferry
        shell: cmd
        run: Build.cmd
