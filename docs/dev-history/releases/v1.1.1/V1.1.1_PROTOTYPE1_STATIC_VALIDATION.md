# Ferry v1.1.1 prototype 1 — Static validation

## Basis
- Source baseline: `Ferry-v1.1.0-final-source.zip`
- Change scope: Explorer-style Paste result selection only, plus prototype version/docs metadata.

## PASS
- All 29 `Source/Ferry/*.cs` files are referenced by `Build.cmd`; no source file is omitted and no nonexistent C# source is referenced.
- Lexical delimiter scan across all 29 C# files found no unmatched `{}`, `()`, or `[]` after comments/string/character literals were excluded.
- `ClipboardHelper.GetPasteSourcePaths()` exists exactly once and captures the Clipboard top-level file-drop list before Paste.
- `MainWindow.Paste(TabState)` exists exactly once and creates a bounded per-tab Paste feedback session before invoking the existing Windows-owned Paste operation.
- Incremental folder reconciliation calls `ApplyPasteFeedbackSelection` after collection mutation, preserving the existing same-path `FileItem` identity design.
- Paste result selection is reconciled to both List and Grid selectors through the existing `ReconcileSelectorSelection` path.
- The logical Selection Anchor / keyboard origin is updated to the first pasted result in current view order.
- Same-folder Copy / Keep-both renamed siblings are matched only when they did not exist before Paste and retain the source basename/stem.
- A successfully completed cross-folder Paste selects the corresponding destination top-level items even when they already existed before the operation (overwrite/merge workflow).
- The Paste feedback session is discarded after the authoritative post-operation refresh, preventing later unrelated watcher refreshes from resurrecting old Paste selection.
- Window title is `Ferry - prototype 1`.
- Assembly/File version is `1.1.1.0`; informational version is `1.1.1-prototype1`.
- `Make-PortableRelease.cmd` is pinned to `1.1.1-prototype1`.
- No `.exe`, `.dll`, `.pdb`, or `dist/` build artifacts are included in this source prototype.

## Runtime gate
This environment cannot run the Windows .NET Framework 4.8 / WPF build or Windows `SHFileOperation`. `Build.cmd` on Windows is therefore the first runtime gate, followed by `V1.1.1_PROTOTYPE1_TEST_JA.md`.
