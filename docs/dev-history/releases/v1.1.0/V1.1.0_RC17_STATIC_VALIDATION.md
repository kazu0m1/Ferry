# Ferry v1.1.0 RC17 — Static Validation

**Candidate:** `1.1.0-rc17`  
**Environment limitation:** Windows / .NET Framework 4.8 runtime is unavailable in the assistant environment. This document records static source validation only; Windows build/runtime PASS requires user testing.

## Version metadata

- [PASS] Window title source = `Ferry - RC 17`.
- [PASS] `AssemblyInformationalVersion` = `1.1.0-rc17`.
- [PASS] `Make-PortableRelease.cmd` version = `1.1.0-rc17`.

## Scope

RC17 changes only Grid keyboard navigation integration around the RC16 fix:

- ordinary Grid `Left / Right / Up / Down` can synchronize Ferry Selection Anchor after WPF navigation;
- empty-selection recovery supports Grid Left/Right as well as Up/Down;
- Grid Up/Down target uses `VirtualizingWrapPanel` visual columns rather than List `index ± 1`;
- List empty-selection target logic remains RC16 behavior.

## Static checks

- [PASS] Grid-vs-List handling uses the active `ctx.GridView` identity.
- [PASS] Grid columns-per-row derives from `VirtualizingWrapPanel.ViewportWidth / ItemWidth`.
- [PASS] Grid directional recovery keeps Left/Right within the visual row and Up/Down in the visual column.
- [PASS] No forced RC12/RC13 diagnostic logging reintroduced.
- [PASS] Rubber-band geometry/code not modified.

## Required runtime validation

See `V1.1.0_RC17_TEST_JA.md`. In particular, Grid background-clear → Down must select the tile visually below the origin, not item index + 1.
